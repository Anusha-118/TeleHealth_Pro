// ==========================================================
// TeleHealth Pro - Deployment Configuration
// ==========================================================
// This is the ONLY file you need to edit when deploying the frontend
// to point at a live backend instead of localhost.
//
// Local development (backend running via `mvn spring-boot:run`):
//   const API_BASE_URL = "http://localhost:8080/api";
//
// Production (backend deployed, e.g. on Render/Railway):
//   const API_BASE_URL = "https://your-backend-app.onrender.com/api";
//
// Must be HTTPS in production - see the "Deployment" section of README.md
// for why (cross-domain session cookies require it).
// ==========================================================

const API_BASE_URL = "https://telehealth-pro-6jk9.onrender.com/api";
